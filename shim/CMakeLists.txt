
# PP4: the managed seam. A shared library, because that is the whole point of it - chiaki-lib is
# a static archive with no exported symbols, and this is what gives the port something to call.
add_library(chiaki-shim SHARED chiaki_shim.c chiaki_shim.h)

target_include_directories(chiaki-shim PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}")
target_link_libraries(chiaki-shim chiaki-lib)

# PP655: the two oracles - nine holepunch wrappers and fifteen json-c ones - follow the libraries
# they wrap, in the BODY and in the HEADER.
#
# The header half is load-bearing. PP437's census holds the host's imports against what these
# headers declare, so gating only the bodies would leave it green while the DLL lost the exports.
# PUBLIC for the same reason: anything compiling against this header has to see the set this DLL
# actually exports. chiaki_shim_has_holepunch and chiaki_shim_has_jsonc are outside both gates and
# are how a managed guard asks which build it got (PP662).
if(CHIAKI_ENABLE_HOLEPUNCH)
	target_compile_definitions(chiaki-shim PUBLIC CHIAKI_SHIM_HAVE_HOLEPUNCH CHIAKI_SHIM_HAVE_JSONC)
endif()

# PP670: the frame path's fourteen wrappers - fec, frame processor, video receiver - are the third
# oracle, and this is PP623's first step for it: the define exists and chiaki_shim_has_framepath
# reports it, so every differential that calls the fourteen can ask before it runs. UNCONDITIONAL
# today, because the four files are still in the build. The flip (PP295's fourth criterion) turns
# this into an if() on its option and wraps the fourteen in the define, in the header and the body,
# and edits no test file - which is what the two-shape readers were converted for.
target_compile_definitions(chiaki-shim PUBLIC CHIAKI_SHIM_HAVE_FRAMEPATH)

# PP25: the nanopb-generated takion.pb.h, which chiaki-lib compiles against but does not publish -
# it adds the directory privately, so a consumer that wants the message types has to name it. Named
# here rather than promoted in lib/CMakeLists.txt, because lib/ is the half this port does not edit.
#
# The dependency on chiaki-pb is what orders it: the header is generated by protoc and the nanopb
# generator at build time, so without this the shim can be compiled before it exists.
target_include_directories(chiaki-shim PRIVATE "${CMAKE_BINARY_DIR}/lib/protobuf")
add_dependencies(chiaki-shim chiaki-pb)

# chiaki-shim.dll and not libchiaki-shim.dll. The name is what a DllImport carries, and a managed
# assembly that has to know it was built by MinGW is a seam that leaked.
set_target_properties(chiaki-shim PROPERTIES PREFIX "" OUTPUT_NAME "chiaki-shim")

# The compiler runtime goes inside, which removes the MinGW DLLs from what has to travel with
# this one. It does NOT make the DLL free-standing, and the first version of this comment said it
# did: chiaki-lib is linked whole-object, so OpenSSL arrived with chiaki_error_string and the
# session functions have since brought curl and the rest with them. chiaki-shim.dll therefore
# imports what the Qt client imports, and the answer is the same one the Qt client has - it is
# copied into the portable tree that already holds those DLLs, and the managed host loads it from
# there rather than from beside its own assembly (see the resolver in app\Native\ChiakiNative.cs).
if(MINGW)
	target_link_options(chiaki-shim PRIVATE
			-static-libgcc
			-Wl,-Bstatic,--whole-archive -lwinpthread -Wl,--no-whole-archive,-Bdynamic)
endif()

# PP9: the renderer's own DLL, and deliberately not part of chiaki-shim.
#
# chiaki-shim.dll is loaded by every run of the selftest, on machines with no GPU and in CI.
# Linking libplacebo into it would make a graphics driver a precondition for parsing a discovery
# reply, and the first symptom would be a test host that cannot start. The two also change for
# different reasons - the protocol seam when libchiaki's surface moves, this when PP9's renderer
# decision does - so one ABI covering both would make every renderer experiment an ABI bump for
# the protocol.
# chiaki_render_dcomp.cpp is C++ and the rest is C, which is measured rather than preferred:
# mingw-w64's dcomp.h does not compile in C at all (PP281 has the four-line reproduction). CMake
# enables both languages from project(chiaki), so the mixed target needs nothing declared here.
add_library(chiaki-render SHARED chiaki_render.c chiaki_render_dcomp.cpp chiaki_render.h)
target_include_directories(chiaki-render PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}")
# d3d11/d3d9/dxgi for the D3DImage share (PP131). Named here rather than assumed present in
# libplacebo's own link line, which does not export them.
#
# dxguid is PP163's, and it is a separate library rather than an oversight: the import libs carry
# the interface THUNKS, and the interface GUIDs live in their own static archive. IID_IDXGIDevice
# and IID_IDXGIResource happen to resolve without it, so the gap only appears the first time a
# newer interface is asked for - IID_IDXGISwapChain3 here - and it appears at link time as an
# undefined reference rather than anywhere a reader would look.
#
# dcomp is PP281's: DCompositionCreateDevice lives in dcomp.dll and nothing above pulls it in.
# IID_IDCompositionDevice comes out of dxguid, already here for the same reason IID_IDXGISwapChain3
# needed it - the GUIDs are a separate archive from the thunks.
target_link_libraries(chiaki-render PkgConfig::LIBPLACEBO d3d11 d3d9 dxgi dxguid dcomp)
set_target_properties(chiaki-render PROPERTIES PREFIX "" OUTPUT_NAME "chiaki-render")

if(MINGW)
	target_link_options(chiaki-render PRIVATE
			-static-libgcc
			-Wl,-Bstatic,--whole-archive -lwinpthread -Wl,--no-whole-archive,-Bdynamic)
endif()
